import CreatePage from "../../crear/page";
import { italianMetadata } from "../metadata";

export const metadata = italianMetadata(
  "Crea con l’IA",
  "Trasforma prompt e materiali interni in contenuti, quiz, simulazioni e valutazioni.",
  "/it/crear",
  "/crear",
);

export default CreatePage;
