import ProductPage from "../../producto/page";
import { portugueseMetadata } from "../metadata";

export const metadata = portugueseMetadata(
  "Plataforma",
  "Crie, orquestre, oriente, meça e melhore experiências de aprendizagem em um único sistema conectado.",
  "/pt/producto",
  "/producto",
);

export default ProductPage;
