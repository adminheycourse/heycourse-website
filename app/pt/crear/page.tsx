import CreatePage from "../../crear/page";
import { portugueseMetadata } from "../metadata";

export const metadata = portugueseMetadata(
  "Criar com IA",
  "Transforme prompts e materiais próprios em conteúdo, quizzes, simulações e avaliações.",
  "/pt/crear",
  "/crear",
);

export default CreatePage;
