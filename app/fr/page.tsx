import Home from "../page";
import { frenchMetadata } from "./metadata";

export const metadata = frenchMetadata(
  "Intelligence d’apprentissage",
  "LXP + LMS avec intelligence d’apprentissage pour transformer les connaissances en compétences démontrables.",
  "/fr",
  "/",
);

export default Home;
