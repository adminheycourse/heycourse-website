import CreatePage from "../../crear/page";
import { frenchMetadata } from "../metadata";

export const metadata = frenchMetadata(
  "Créer avec l’IA",
  "Transformez vos prompts et vos ressources en contenus, quiz, simulations et évaluations.",
  "/fr/crear",
  "/crear",
);

export default CreatePage;
