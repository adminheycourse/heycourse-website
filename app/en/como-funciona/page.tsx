import HowItWorksPage from "../../como-funciona/page";
import { englishMetadata } from "../metadata";

export const metadata = englishMetadata(
  "How it works",
  "From one prompt to a measurable learning experience: create, publish, guide, observe and intervene.",
  "/en/como-funciona",
  "/como-funciona",
);

export default HowItWorksPage;
