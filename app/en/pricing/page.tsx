import PricingPage from "../../pricing/page";
import { englishMetadata } from "../metadata";

export const metadata = englishMetadata(
  "Pricing",
  "Organization-based plans with unlimited learners and AI credits for creation, tutors, voice, avatars and simulations.",
  "/en/pricing",
  "/pricing",
);

export default PricingPage;
