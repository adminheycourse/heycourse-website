import ProductPage from "../../producto/page";
import { englishMetadata } from "../metadata";

export const metadata = englishMetadata(
  "Platform",
  "Create, orchestrate, guide, measure and improve learning experiences in one connected system.",
  "/en/producto",
  "/producto",
);

export default ProductPage;
