import CreatePage from "../../crear/page";
import { englishMetadata } from "../metadata";

export const metadata = englishMetadata(
  "Create with AI",
  "Turn prompts and source material into structured content, quizzes, simulations and assessments.",
  "/en/crear",
  "/crear",
);

export default CreatePage;
